[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    api: API-level tests (no browser needed)
    browser: Browser-based tests (Playwright)
addopts = -v --tb=short
